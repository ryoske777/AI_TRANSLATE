# RO 로컬라이제이션 도구 — 외부 의존성
# 설치: pip install -r requirements.txt
# (tkinter 는 표준 라이브러리이며 OS 파이썬에 포함되어 있어야 함)

customtkinter>=5.2.0      # 데스크톱 UI
selenium>=4.10.0          # ChatGPT/Claude 웹 UI 자동화
gspread>=5.10.0           # 구글 시트 읽기/쓰기
google-auth>=2.20.0       # 구글 서비스 계정 인증 (google.oauth2)
pyperclip>=1.8.2          # 클립보드 기반 텍스트 입력
